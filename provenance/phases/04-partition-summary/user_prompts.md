# User prompts for 04-partition-summary

Extracted from transcript `fec1d6d6-3e05-4db6-99ec-6d712cea59ee.jsonl`. User-message indices 14 through 17.

## User prompt #14
_Saturday, May 2, 2026, 8:16 AM (UTC-4)_

```
Make a plan for another stand-slone python prgroam. This should produce a selection summary of the impacts by partition. For each partition it should show total number of nodes before and after the reduction (this can include all nodes - not just R9 nodes). It should also show other information gleaned from scontrol_show_node.json. The other information should be CPU counts before and after and GPU counts organized by type (e.g. H100, A100, L40S etc...) before and after. All this should be written to a spreadsheet. This program can also be run as part of the stage that generates  selection_summary.csv, the new file could be
called selection_summary_by_partition.csv by default.
```

## User prompt #15
_Saturday, May 2, 2026, 8:18 AM (UTC-4)_

```
# Goal

Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

To-do's from the plan have already been created. Do not create them again. Mark them as in_progress as you work, starting with the first one. Don't stop until you have completed all the to-dos.
```

## User prompt #16
_Saturday, May 2, 2026, 8:30 AM (UTC-4)_

```
Implement the plan as specified, it is attached for your reference. Do NOT edit the plan file itself.

To-do's from the plan have already been created. Do not create them again. Mark them as in_progress as you work, starting with the first one. Don't stop until you have completed all the to-dos.
```

## User prompt #17
_Saturday, May 2, 2026, 8:30 AM (UTC-4)_

```
Can you modify the program so that only partitions with nodes removed are reported.
Also see if you can compact/shorten heaings so that more fits on a page when viewd with "csvlens" a handy CSV viewer. The current view is too wide to see easily ( see image).
```

